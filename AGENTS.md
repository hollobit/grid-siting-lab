# 에이전트 활용 및 작업 규칙

## 적용 범위와 실행 환경

- 이 파일은 AIDC 프로젝트의 제품·데이터·검증 규칙을 정의한다. 공통 작업 방식과 스킬 호출은 상위 지침을 따른다.
- 서브에이전트 사용 가능 여부는 매 세션 실제 노출된 도구로 확인한다. 독립적이고 범위가 명확한 작업은 이점이 있을 때 네이티브 서브에이전트에 위임하며 지원되는 `agent_type`을 지정한다.
- 위임 도구가 없으면 메인 에이전트가 수행하고 독립적인 읽기 전용 점검은 병렬 도구 호출로 처리한다.
- 네이티브 서브에이전트와 tmux 기반 OMX 팀 런타임은 별개다. 현재 환경에서 지원되는 실행 방식만 사용한다.

## 역할 분리

### Main / Product

- 사용자 요구사항을 제품 구조와 UI 흐름으로 번역한다.
- 점수 모델과 시뮬레이션의 의미를 결정하고, 확정 데이터와 프록시 데이터를 구분한다.

### Data / Research

- OpenInfraMap·OSM·공공 데이터 출처와 날짜를 확인한다.
- 도시, 교통, 철도, 공항, 통신, 위성, 클라우드/IDC, 대학/R&D 레이어의 데이터 연결 지점을 기록한다.

### Frontend / Visualization

- 지도·레이어·후보지 카드·시나리오 패널을 구현한다.
- Leaflet 외부 리소스가 실패해도 fallback map이 동작하도록 한다.

### QA / Handoff

- 정적 서버 실행, 콘솔 오류, 반응형 레이아웃, 후보지 선택, 슬라이더, 레이어 토글을 검증한다.
- 결과·한계·다음 데이터 연동 지점을 `HISTORY.md`에 남긴다.

## 병렬 작업 규칙

- 읽기 전용 검색/점검은 병렬 처리한다.
- 같은 파일을 여러 작업이 동시에 수정하지 않는다.
- 파일 변경은 `apply_patch`로만 수행한다.
- 각 의미 있는 변경은 `HISTORY.md`에 짧게 기록한다.
- 새 외부 데이터는 화면 문구·README·출처 링크에 반영한다.

## 스킬과 검증

- 스킬은 현재 카탈로그의 실제 `SKILL.md` 경로에서 읽는다. 사용자 전역 규칙이나 외부 스킬 목록을 이 파일에 복제하지 않는다.
- 앱 변경 시 `node --check app.js`와 `npm run dev`를 이용해 검증하고, UI 변경은 가능한 브라우저 도구로 확인한다.
- `npm test`는 3D 규모 모델 회귀검사, `npm run check`는 JavaScript 구문 검사, `npm run build`는 배포용 정적 산출물을 검증한다. lint·typecheck는 설정되어 있지 않으므로 실행한 것으로 보고하지 않는다.
- 지침·스킬 문서만 변경하면 메타데이터, 참조 경로, 지침 충돌과 `git diff --check`를 검증한다.
